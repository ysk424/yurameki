# Standalone Python env to mirror Blender 5.1's BPY (Python 3.13) for the
# Blender-independent simulation server. Install into the bare Python 3.13:
#   python -m pip install -r requirements.txt
#
# warp-lang is the production CUDA path (bundled in Blender 5.1 as 1.13.0).
warp-lang==1.13.0
numpy>=2.3
scipy
